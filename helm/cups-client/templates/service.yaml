apiVersion: v1
kind: Service
metadata:
  name: {{ include "cups-client.fullname" . }}
  labels:
    {{- include "cups-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cups-client.selectorLabels" . | nindent 4 }}
